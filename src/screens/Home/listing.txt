{/* {sortedListings.length > 0 && (
                        <>
                            <Text className="text-2xl font-bold my-4">Other Listings</Text>

                            <View className="mb-20">
                                {sortedListings.map((item, index) => {
                                    const cardTitle = item.platform_name || 'Unknown Platform'
                                    const price = item.total_price ?? Number(item.price)
                                    const isLowest = index === 0
                                    const platformLogo = getPlatformLogo(cardTitle)
                                    const action = getListingAction(item)

                                    return (
                                        <View
                                            key={String(item.id ?? index)}
                                            style={[
                                                styles.listingCard,
                                                isLowest && styles.listingCardBest,
                                            ]}
                                        >
                                            {isLowest && (
                                                <View style={styles.bestPriceBadge}>
                                                    <MaterialIcons
                                                        name="star"
                                                        size={12}
                                                        color="#fff"
                                                    />
                                                    <Text style={styles.bestPriceText}>
                                                        Best Price
                                                    </Text>
                                                </View>
                                            )}

                                            <View style={styles.listingRow}>
                                                <View style={styles.logoBox}>
                                                    {platformLogo ? (
                                                        <Image
                                                            source={platformLogo}
                                                            style={styles.logoImage}
                                                            resizeMode="contain"
                                                        />
                                                    ) : (
                                                        <MaterialIcons
                                                            name="storefront"
                                                            size={28}
                                                            color="#2355B6"
                                                        />
                                                    )}
                                                </View>

                                                <View style={styles.listingInfo}>
                                                    <Text style={styles.listingPlatformName}>
                                                        {cardTitle}
                                                    </Text>
                                                    <Text style={styles.listingShipping}>
                                                        {item.free_shipping
                                                            ? 'Free Shipping'
                                                            : `Shipping: $${item.shipping_cost || '0'
                                                            }`}
                                                    </Text>
                                                </View>

                                                <View style={styles.listingRight}>
                                                    <View style={styles.priceCol}>
                                                        <Text
                                                            style={[
                                                                styles.listingPrice,
                                                                isLowest && styles.listingPriceBest,
                                                            ]}
                                                        >
                                                            ${price.toFixed(2)}
                                                        </Text>

                                                        {!!item.original_price &&
                                                            Number(item.original_price) > 0 && (
                                                                <Text style={styles.listingOriginal}>
                                                                    $
                                                                    {Number(
                                                                        item.original_price
                                                                    ).toFixed(2)}
                                                                </Text>
                                                            )}

                                                        {!!item.discount_percentage &&
                                                            Number(item.discount_percentage) > 0 && (
                                                                <Text style={styles.listingDiscount}>
                                                                    -
                                                                    {Math.round(
                                                                        Number(
                                                                            item.discount_percentage
                                                                        )
                                                                    )}
                                                                    %
                                                                </Text>
                                                            )}
                                                    </View>

                                                    {action === 'view' ||
                                                        isKnownPlatform(cardTitle) ? (
                                                        <TouchableOpacity
                                                                style={[
                                                                    styles.listingBtn,
                                                                    !isLowest &&
                                                                    styles.listingBtnSecondary,
                                                                ]}
                                                                onPress={() =>
                                                                    openUrl(item.external_url)
                                                                }
                                                            >
                                                                <Text
                                                                    style={[
                                                                        styles.listingBtnText,
                                                                        !isLowest &&
                                                                        styles.listingBtnTextSecondary,
                                                                    ]}
                                                                >
                                                                    {isLowest ? 'Buy Now' : 'View'}
                                                                </Text>
                                                            </TouchableOpacity>
                                                    ) : (
                                                        <TouchableOpacity
                                                            style={[
                                                                styles.listingBtn,
                                                                isInCart &&
                                                                styles.listingBtnCart,
                                                            ]}
                                                            onPress={handleAddToCart}
                                                            disabled={isInCart || cartLoading}
                                                        >
                                                            {cartLoading ? (
                                                                <ActivityIndicator
                                                                    size="small"
                                                                    color="white"
                                                                />
                                                            ) : (
                                                                <Text style={styles.listingBtnText}>
                                                                    {isInCart
                                                                        ? '✓ In Cart'
                                                                        : 'Add to Cart'}
                                                                </Text>
                                                            )}
                                                        </TouchableOpacity>
                                                    )}
                                                </View>
                                            </View>
                                        </View>
                                    )
                                })}
                            </View>
                        </>
                    )} */}